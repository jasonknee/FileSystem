public class FileTable {
    FileTable[] table;
    Directory directory;

    public FileTable(Directory dir) {
        // table = new Vector();
        directory = dir;
    }

    // getFileEntryAtIndex(){}
}